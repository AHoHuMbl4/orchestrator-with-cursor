# Ловушки оркестрации — читать перед пачкой

Формат: ловушка → признак → обход. Встретил новую — добавь строку в таблицу.

| # | Ловушка | Признак | Обход |
|---|---|---|---|
| 1 | Промт-текст литералом в argv/URL облачного запуска | гейты/шелл/логи ловят слова из промта; кавычки ломают команду | промт только из файла: `run-cloud.py … --prompt-file P.md` (тело API — `prompt={"text":…}`) |
| 2 | Свобода в промте («или другой способ») | исполнители делают разное, сводки расходятся | путь/инструмент/команда дословно |
| 3 | Вера сводке исполнителя | факты в отчёте врут при верном результате | приёмка только замером (файл/тест/счётчик лога / `run-cloud.py artifacts`) |
| 4 | Один прогон — много задач | поздние задачи делаются хуже | 1 задача = 1 агент; пачка — параллельно |
| 5 | Критик видит лог исполнителя | критик наследует его заблуждения | критик получает только дифф + критерий |
| 6 | Параллельные пишущие исполнители на одной области | конфликты, несогласованные решения | пишет один; параллельные — read-only |
| 7 | `pkill/pgrep -f` с подстрокой промта | агент убивает сам себя (промт в argv) | уборка процессов по PID, без масок по тексту |
| 8 | Многострочный код через `-c` в обёртках | ложные выводы от экранирования | диагностика heredoc-файлом |
| 9 | Тихий exit 0 у headless-запусков (`claude -p`) | «готово», а действия не было: отказы прав не меняют exit code | проверять поле `permission_denials` в json/result; `--permission-prompts none` (старые сборки — `--permission-mode dontAsk`) |
| 10 | Модели/флаги из памяти | неверный флаг или модель — прогон впустую | снимать списки с движка перед боем |
| 11 | Повышение таймаута вместо декомпозиции | частые таймауты / долгий polling cloud | дробить задачу; прогресс — подшагами |
| 12 | Запись задачи в общий compass / дрейф цели | агент или CLI пишет в `.orchestration/compass.md` вместо сессионного | задача пишется ТОЛЬКО в сессионный compass; общий шаблон редактируется только владельцем в панели (Расширенные); CLI/global-пути записи запрещены; сессионный compass создаётся автоматически из шаблона при первом сообщении сессии |
| 13 | Отчёт без строки-вердикта или без доказательств | «я сделал» текстом; нет `Вердикт: OK|PROBLEMS|BLOCKED` и блока команд/артефактов | непринят; приёмка только по вердикт-конвенции + замер оркестратора |
| 14 | Оркестратор сам сшил финальный ответ в своём контексте | доклад без волны синтезаторов/критиков по артефактам | синтез — отдельная волна через критиков; оркестратор только выбирает лучший |
| 15 | Декомпозиция без стоп-вопросов при неоднозначном запросе | пачка стартовала без предмета/критерия готовности | СТОП, 1–3 вопроса или Assumptions; иначе мусор на выходе |
| 16 | Код-задачу погнали в облако при живом локальном CLI | роль из `code/`, а запуск через `run-cloud.py` / cursor-cloud, хотя `cursor-agent` в PATH есть | ошибка маршрутизации: облако не имеет ФС, кураторство контекста искажает фундамент → код → `run-exec.py` (local-cursor); cloud-код — только явный fallback при недоступности CLI (`engines.md`) |
| 17 | `resource_exhausted` на create cloud-агента (burst-лимит попыток) | create вернул `resource_exhausted` («Rate limit exceeded for creating cloud agent environments», `isRetryable`); иногда маскируется как 500 internal | это лимит попыток создания, не «подождать 5 мин и долбить»: пауза ≥15–30 мин, повтор одиночный; при повторе — экспоненциальный рост паузы. Cron/молотилка каждые 5 мин держит лимит бесконечно — см. `engines.md` |
| 18 | create cursor-cloud завис/умер по таймауту | create оборвался по wall-clock, а агент уже мог появиться на сервере | сначала recovery через список агентов (`run-cloud.py` умеет); не слепой ретрай create — дубликаты жгут квоту |
| 19 | Фоновый лаунчер движка сгорел на своём таймауте (600 с и др.), статус прогона неизвестен | длинный прогон держали в Bash-фоне движка, а не в `--detach`; движок убил лаунчер → pid/exit неизвестны, логи ищут руками | ВСЁ дольше ~8 мин — ТОЛЬКО `run-exec … --detach` (мгновенный pid+log; переживает смерть Bash-сессии) или `run-cloud run --wait` в фоне с запасом; контроль — `run-exec --id X --status` (жив/exit/вердикт/лог); инвентарь — `run-exec --list`; foreground/`--wait` — только короткие прогоны; или ничего не угадывай: авто-уступка на 480 с (--yield-after, 0 = выкл) сама уведёт прогон в фон; признак проблемы исчезает |
| 20 | Агент «ускорил» механику: `ls` вместо каталога `_index.md`, промт без шаблона роли, поиск инструмента по ФС вместо MAP | нет строки `Механика:` в отчёте волны; роль/путь взяты «из головы» или обходом каскада | механика не опциональна; строка `Механика:` в отчёте обязательна; отступление без фиксации в compass = ошибка курса; см. SKILL «Механика не опциональна» + `references/MAP.md` |
| 21 | HTTP 400 «limit for Cloud Agents simultaneously» | create/run: лимит **текущего** плана на одновременно идущие cloud-прогоны (не burst №17, не квота); тариф в тексте Cursor — часть сообщения, не признак; слоты освобождаются с грейсом | тишина ~10 мин, затем одиночные ретраи с ростом паузы; параллелизм навсегда не снижать |
| 22 | Схемы AskUserQuestion различаются между движками — не выдумывай поля, не переноси привычки из другого движка | вызов инструмента вопросов упал по валидации схемы (лишнее/чужое поле, например multiSelect) | ОДИН повтор с минимальным набором (question + options [+ header]); повторный фейл → вопросы недоступны → лестница авто-режима (Assumptions → compass → доклад) |
| 23 | Критики синтеза массово жалуются на «новые факты» при обрезанных входах | проверка полноты / «0 новых фактов» на выжимке ловит обрезанное, а не выдуманное | пометка в промте («ВЫЖИМКИ полных…»); запрет проверки новых фактов на выжимках; grep-фильтр жалоб по полным артефактам ДО ремонта (см. SKILL «Обрезка входов») |
| 24 | Код-облако правит по неполным выдержкам без манифеста/дозапроса | дифф собран «на глаз» из фрагментов; нет манифеста или стоп-точки `НЕ ХВАТАЕТ` | отбор контекста посредником искажает фундамент → манифест + пометки `ФРАГМЕНТ` + стоп-точка `НЕ ХВАТАЕТ` + локальный замер; см. `engines.md` «Код-задачи через облако» |
| 25 | Генерал на дешёвой модели | ошибка верхнего уровня множится на весь фронт | генералы — только умные движковые субагенты; cursor — полковники, свита, наблюдатели, прокурор, исполнители (проверочные и механические роли) |
| 26 | Волна графа без контрольной точки наблюдателей / план графа/фронта без волны критиков / нетривиальный план задач без волны | следующая волна графа стартовала без OK наблюдателей; или план графа/фронта пошёл без критиков; или нетривиальный план задач без волны критиков | Запрещено стартовать волну графа без OK наблюдателей (контрольная точка обязательна); планы графа и фронта — всегда безусловно волна критиков; уровень задач — волна ОБЯЗАТЕЛЬНА при нетривиальном плане (более 2-3 задач, либо чужие файлы/швы/контракты); тривиальный (1-2 задачи в своих границах) — чек-лист без волны (см. SKILL / planning §3.7) |
| 27 | Откат косяка самодельным скриптом/ручными правками вместо git; пишущая волна без чекпоинта | нет точки возврата; «починка поверх» грязного дерева; история/статус вранья | до пишущей волны — чекпоинт `code/git-warden` (коммит/тег `orch/wave-<N>`); косяк — только `git reset` к чекпоинту или `git revert`; после волны — ревизия git-warden (см. SKILL «Гит-дисциплина») |
| 28 | На пишущую задачу запущено N>1 исполнителей по `parallel_per_task` | параллельные писатели одного кода дают дубли и конфликты; параметр — для read-only | 1 писатель + 3 критика после; spike — отдельное осознанное решение в изолированных ветках |
| 29 | Генерал сам исполняет работу исполнителя (ручные правки вместо запуска волн) | дрейф роли у субагента без реграунда; Write/Edit проекта = нарушение с первого раза (не «после трёх»); длительность — фактор риска оркестрации, не лицензия на руки | правило НОЛЬ подшагов + исчерпывающий список инструментов генерала (а)–(д); границы работ как точки сверки + строка самопроверки; НЕ рвать по таймеру (см. planning §3.7 / front-general) |
| 30 | Замещение вниз рушит всю иерархию проверок (нет критиков над самоделкой, нет слепоты, нет приёмки) | у исполнителя/генерала кончилась квота или лимит, а вышестоящий уровень начал делать его работу руками | СТОП: пауза → доклад владельцу (в сессии без вопросов — лестница: пауза + доклад текстом + разреженные ретраи) → продолжение после решения владельца/восстановления квоты; разрешённые компенсации: retry/follow-up/новый слепой агент (генерал/исполнитель) по тому же compass — НИКОГДА ручное выполнение |
| 31 | Генерал на развилке «как делать» решил вслепую: не запустил советников, вариантов <2, обоснования нет | дыра доставки — правило было в SKILL, которого генерал не читает | шаг советников вшит в шаблон front-general (проверять при сборке промта, не вырезать); совет не обязателен только с явной пометкой «без советников, потому что …» |
| 32 | Проверка проводки новой механики шла по чек-листу файлов («есть ли строки в этих N файлах»), а не по пути доставки до адресата | чек-лист наследует слепое пятно автора задания — дыра проходит проверку гарантированно (живой случай: советники были в SKILL/каталоге, но генерал читает только свой шаблон роли — шаг до него не доходил) | вопрос ревьюера: «кто АДРЕСАТ правила и через какой документ он его реально получает?» — пройти путь доставки целиком (адресат → что он читает → есть ли правило там); проверка проводки без этого вопроса — неполная |
| 33 | В промт проверяющему вписаны локации/строки/ожидаемые выводы (карта реализации) | проверка наследует слепое пятно составителя — независимость потеряна, дыра проходит гарантированно (живой случай: аудиту советников дали список из 4 файлов — шаблон роли адресата в список не входил) | артефакт+критерий+адресат, без карты; если проверяющему нужен доступ — дай путь к системе целиком, а не к выбранным местам |
| 34 | Потеря параллельности и размывание специализации | командующий loaded одного генерала многими задачами / фронты одной волны запускаются последовательно | 1 генерал = 1 фронт, волна графа стартует целиком |
| 35 | Контекстное загрязнение, снайпер теряет фокус | уровень разбирает нюансы чужого уровня вместо ответа «нет» и передачи вниз | ответ по своему критерию, передача вниз с критерием, разбор на уровне с контекстом |
| 36 | Разрушение слепоты и иерархии внутри волны | параллельный генерал читает вердикты чужих критиков / останавливает чужие или свои прогоны по чужим результатам / координируется с параллельной линией напрямую | границы путей в промте + прокурор внутри волны + вся coordination только через командующего; дубли фронтов — чинить наверху (граф), не останавливать самовольно внизу |
| 37 | Полковник без свиты | дешёвая cursor-модель командует подзадачей без критиков плана, критиков приёмки, читателей сырья и советников; ошибка командира множится на весь куст работ | свита обязательна (таблица этап→роль в planning.md); план без «Вердикт: OK» не стартует |
| 38 | Роль с головы | роль придумана вне каталога и вне фабрики кита; ломает каскад и учёт | только `_index.md`; нет роли → фабрика по `_template.md` с атрибуцией |
| 39 | Пакет задач одному генералу | командующий грузит одного генерала несколькими фронтами/задачами сразу; генерал тонет, контекст растёт, параллельность умирает | 1 приказ = 1 фронт = 1 генерал; масштаб = число генералов; прокурор помечает пакетную загрузку |
| 40 | Хук пережил удаление | удалитель снимает только маркированные блоки хуков; старые немаркированные reground-хуки остаются в config.toml и стреляют в несуществующий путь после сноса кита | зачистка всех хуков по содержимому command (reground.py), не только по маркерам (починено в uninstall) |
| 41 | Роль в MAP, но без проводки | роль числится в карте, но ни одна доктрина её не вызывает (пример: simplicity-warden до этого фикса); мёртвый узел создаёт иллюзию гарантии | аудит проводки; каждая роль MAP должна иметь вызывающего (доктрина/роль) или быть явно помечена «по требованию владельца» |
| 42 | Старый чат после обновления | владелец обновил кит и продолжил тот же чат; рестарт перерегистрировал скилл, но история чата с прежними паттернами перевешивает свежую доктрину — командующий работает по-старому, новые роли/гейты не вызываются | новая сессия с входом с диска (PROJECT.md → компас → fronts.json → handoff); в длинном чате — явная директива «перечитай SKILL/MAP с диска, противоречия истории отменены»; обновления применять на границе волны |
| 43 | Секрет в промте: ключ/токен вклеен в текст промта исполнителя → утечёт в облако/логи | SECRETS_IN_PROMPT в логе, exit 5 | секреты только в `.orchestration/cursor.key` / ENV; обёртки сканируют и отказывают |
| 44 | Обновление посреди волны: доктрина сменилась, пока фронт в полёте; смена правил на лету сиротит ходы и путает агентов | доктрина/правила меняются при живых фронтах / волне в полёте | pin версии на волну; новое — со следующей волны; хук предупреждает при живых фронтах |
| 45 | Фронт-зомби: фронт отменён (cancelled), но прогоны продолжают запускаться/полковник не знает | FRONT_CLOSED в логе; чип статуса в панели | `--front` у прогонов, обёртки закрывают запуск, командующий сообщает полковнику |
| 46 | Вызов без летописца | агент запущен в обход обёрток run-exec/run-cloud (журнал не растёт, дерево рвётся) | признак — лог прогона есть, а в journal.jsonl нет НИ start, НИ end для этого id; gate-отказы (exit 5/6/7) пишут start+end — это НЕ ложный сигнал ловушки 46; обход — запуск только через обёртки |
| 47 | Приказ в компасе | курс и приказ в одном файле — перезапись курса стирает приказ (кейс: потерян CANON1); генерал «потерял» приказ после ужатия компаса | приказ — в `order.md` (read-only); компас — только курс со ссылкой; сверка курса — с `order.md` |
| 48 | Мимо воронки | агент пишет компас напрямую Write/Edit в обход `write-compass.py`; в сессиях с хуками PreToolUse блокирует с причиной «только через воронку write-compass.py»; без хуков / внешние писатели — пост-фактум-гард (данные уже могли уйти); признак: COMPASS ПРЕВЫШЕН при живом фронте / повторное раздувание / deny PreToolUse в логе | компас — только через `write-compass.py` (всем уровням с shell); PreToolUse — мгновенный блок при хуках; пост-фактум-гард + прокурор/инспектор — для сессий без хуков и внешних писателей |
| 49 | Развилка без советников | командир любого уровня (включая командующего) решает «как делать» вслепую; признак: значимое решение без advisor-записей в journal.jsonl и без пометки «без советников, потому что» | правило вариантов<2 → советник; чек-лист статуса командующего; приёмка фронта с замером по журналу |
| 50 | Генерал руками | командир фронта сам правит файлы — контекст тонет, качество падает, иерархия вырождается в одного агента; счёт собственных подшагов ненадёжен у любой модели под контекстным давлением; признак — активный фронт с меняющимися компасом/файлами при пустом журнале прогонов | правило НОЛЬ подшагов (инструменты генерала — исчерпывающий список); детект инспектором/наблюдателем по журналу; чек-лист статуса командующего |
| 51 | Дублирование выживших волн | после зависания/снятия окна командир перезапускает уже идущие серверные волны → двойная работа, гонки, перерасход; признак: новые start с той же ролью/фронтом при живых start-без-end | возобновление через окно приёмки по журналу; директива нового генерала; инспектор помечает зомби |
| 52 | Приказ без советника | командир пишет приказы без проверки подхода; «очевидно» решается в голове; выбор вслепую множится на подзадачу | признак: `order.md` без строки подхода / «без советников»; журнал фронта без advisor-записей при живых приказах; обход: советник перед приказом при любом выборе; строка обоснования в каждом приказе; прокурор/инспектор помечают |
| 53 | Субагент невидим | движковые генералы-субагенты не попадали в журнал летописца (видны были только запуски через обёртки run-exec/run-cloud); признак старый: фронт active, файлы меняются, journal пуст | хуки SubagentStart/SubagentStop пишут генералов-субагентов движка в `journal.jsonl`; сверять journal при приёмке фронта |
| 54 | Ложная метка роли | запуск без `--role`/шапки `роль:`, или путь роли только в теле промта → журнал врёт, аудит слепнет, инспектор строит кривое дерево; признак: `role=null` у рабочей задачи; роль в journal/audit не соответствует задаче | первая строка промта `роль: <путь>` + `--role` у обёртки; extractor смотрит только шапку (первые 3 строки), тело не считается |
| 55 | Руки — не сдача | командир пытается сдать работу, сделанную сам; журнал пуст — приёмка отказывает. Признак: фронт `active`, доклад есть, journal без записей исполнителей | слои B+C + чек-лист + PreToolUse-deny компаса: приёмка только по журналу; короткие окна генерала; командирные роли с артефактами — обычный запуск (без `--readonly`); `--readonly` — только аналитика «чтение+выжимка в ответ» |
| 56 | Чужой state | новая сессия/проект продолжает чужой граф фронтов (state найден вверх по дереву); признак: session-entry показывает чужую цель/фронты, вклейка «STATE БЕЗ ПРОЕКТА»; подпапка при наличии PROJECT.md у родителя — молчаливый подхват; защита: индикация "State найден выше cwd" в session-entry + предупреждение STATE РОДИТЕЛЯ | папка проекта верхнего уровня, PROJECT.md обязателен, проверка входа |
| 57 | Ловит только владелец | система молчит; нарушения всплывают только от личного контроля владельца. Признак: нарушение найдено вопросом владельца; в журнале окна нет прокурора/инспектора; приказы без строк обоснования | механические детекторы (детекторы + нуджи); прокурор на каждую волну обязателен и проверяем журналом |
| 58 | Промт-надежда вместо старт-гейта | «в промте написано --front» / «агент сам укажет фронт» — обход без жёсткого отказа до старта | `FRONT_REQUIRED` (exit 8) в `run-exec`/`run-cloud` через `resolve_front_launch`; `--front` или `--no-front "<причина>"`; hierarchy=off → `no_front_reason=hierarchy-off` |
| 59 | Прокурор, которого никто не звал | волна закончилась, прокурора нет; надежда на «командующий вспомнит» | автопрокурор после end роли волны (`maybe_auto_prosecutor_after_end`, lockdir один на волну); чип `fronts_no_prosecutor`; инспектор |
| 60 | Пуш после ремонт-фикса без второго круга критиков | замер командующего (grep, curl, py_compile) принят за приёмку; пуш после ремонта/docs-keeper без OK слепых критиков (случай: 67f7726 pend-волна — один grep-замер, второй круг задним числом дал OK, порядок нарушен) | после любого ремонт-фикса обязателен второй круг слепых критиков — только их OK открывает пуш; замер командующего проверяет узкий список, не итоговое состояние |
| 61 | Маска EXIT=0 | код выхода 0 принят как готовность; в тексте отчёта BLOCKED / обрезка / «частично», а приёмка зелёная; часы потерь на «успешный» фейл (кейс DR 2026-09-26) | EXIT=0 ≠ готовность; блокирующие маркеры в отчёте обязаны ломать успех; приёмка — по вердикту и замеру из содержимого, код выхода только сортировка |
| 62 | ETA без темпа | в приказе/докладе ETA или «успеем к N» без цифр темпа из journal; «розовый» прогноз кодифицирован в order.md | прогноз в приказе допустим только от замеренного темпа (N задач/час по журналу); иначе честно «темп не замерен» — и это не ETA |
| 63 | Обрезка молча | финальный ответ у потолка/обрезан, обёртка молчит с EXIT=0; зная об обрезке, ушли в симптоматических писателей вместо эскалации | обёртка меряет размер финального ответа и предупреждает у потолка; знание «обрезает» = обязательный архитектурный эскалейт; симптоматические обходы — ловушка |
| 64 | Решение до диагноза | горящий датчик без окна приёма; пауза без параллельного поиска корня; ультиматум на нагрузку до диагноза — часы тратятся впустую | запрещённые состояния командира; диагноз идёт параллельно всегда; при горящем датчике — окно приёма обязательно |
